# quant-desk에서 일하는 방법

## 절대 규칙

1. **주문·한도·손절 경로에 LLM 판단을 넣지 않는다.** 이 경로는
   `core/risk/limits.py`, `core/execution/orders.py`, `core/pipeline.py`,
   `.claude/hooks/pretrade_gate.py`다. 여기에 "에이전트가 판단해서 통과시키는"
   분기를 만들지 않는다.
2. **수치는 산출물에서만 인용한다.** 리포트·PR·커밋 메시지의 모든 숫자는
   `{{artifact:<run_id>/<metric>}}` 참조를 함께 쓴다. 직접 타이핑한 숫자는 무효다.
3. **`core/risk/limits.yaml`을 고치지 않는다.** 한도 변경은 사용자 승인 사항이고
   감사로그에 남는다. 게이트를 통과시키려고 기준을 낮추는 변경은 반려된다.
4. **fail-closed를 우회하지 않는다.** 데이터 헬스 실패, 미해결 주문, 리컨 break는
   신규 주문을 막는다. 예외 경로를 추가하지 않는다.
5. **센터북을 우회하지 않는다.** 주문은 `core/portfolio/center_book.py`의 넷팅을
   거친 뒤 pre-trade 훅으로 간다. 포드 주문을 직접 시장에 내보내는 경로를 만들지
   않는다. 오버레이는 포지션을 줄이기만 하며, 키우는 기능은 cio 승인 사항이다.
6. **포드 전용 피처를 만들지 않는다.** 피처는 `feature-factory` 카탈로그에
   등록되어 전 포드가 읽는다. 특정 포드만 쓰는 비공개 피처는 반려된다.
7. **캐패시티 상한을 넘겨 배분하지 않는다.** 추정 캐패시티의 80%가 상한이고,
   성과가 좋다는 이유로 올리지 않는다.
8. **캐너리를 통과시키지 않는다.** 게이트 기준을 바꾸는 변경은
   `tests/canaries/`를 다시 통과해야 한다.

## 코드 규칙

- Python 3.11+ (CI는 3.12), `uv`로 의존성 관리, `ruff`로 lint·format.
- 새 기능에는 실패 경로 테스트를 함께 쓴다. 한도·복구·게이트는 특히 "막히는지"를
  테스트한다.
- 주변 코드의 주석 밀도와 명명을 따른다. 설명이 필요한 곳은 왜 그렇게 했는지를
  쓴다(무엇을 하는지는 코드가 말한다).

## 커밋·PR

- 커밋은 작게, 한 가지 변경만. PR 본문은 Before/After 문단으로 시작한다.
- 게이트 기준, 한도, 에이전트 권한을 건드리는 PR은 근거를 ADR로 남긴다.

## 기록

판단 근거는 `registry/`에 남긴다. 기록되지 않은 판단은 일어나지 않은 것으로 본다.
