---
name: alpha-gate
description: 알파를 G0부터 G8까지 심사하는 절차와 정량 기준. 알파를 제출·검증·승인할 때, 게이트 기준을 확인할 때 읽는다.
---

# 알파 게이트 G0–G8

기준값은 `core/risk/limits.yaml`의 `gates:` 블록이 단일 출처다. 이 문서와 값이
어긋나면 YAML이 맞다. 판정은 에이전트의 문장이 아니라 결정론적 테스트의
반환코드이며, 결과는 `registry/alphas/<id>.yaml`에 append-only로 쌓인다.

| 게이트 | 담당 | 통과 기준 |
|---|---|---|
| G0 데이터 적격성 | data-quality | as-of 정합 통과, 룩어헤드 스캔 0건, 최소 히스토리(주식 10년/선물·ETF 15년), 서바이버십 보정 |
| G1 가설 사전등록 | 포드 → cio | 경제적 근거 1페이지, 파라미터 범위·평가지표 사전 고정, 이후 시행횟수 N 자동 카운트 |
| G2 인샘플 | 포드 | 비용 반영 IS 샤프 ≥ 1.0, 회전율 차감 순알파 > 0 |
| G3 OOS·교차검증 | backtest-engineer | purged K-fold + embargo, 워크포워드. OOS 샤프 ≥ 0.5 × IS, 폴드 부호 안정성 ≥ 70% |
| G4 통계 | adversarial-validator | DSR ≥ 0.95, PBO < 5%, 블록 부트스트랩 p < 0.05, **팩터 잔차 알파 t ≥ 3.0**, 최대DD/연수익 ≤ 2.0 |
| G5 강건성 | adversarial-validator | 파라미터 ±20%에서 샤프 열화 < 30%, 비용 2배에서 순수익 > 0, 체제별 붕괴 없음 |
| G6 캐패시티·중복 | microstructure-research | ADV 참여율 ≤ 3%, 임팩트 반영 수익 곡선, 기존 북 상관 \|ρ\| ≤ 0.3 |
| G7 페이퍼 | execution-trader + model-risk | 60거래일 무중단. **성과 게이트가 아니다** |
| G8 실자본 | 사용자 | 목표 자본 10% → 3개월 무사고 → 25% → 50% → 100% |

## DSR은 확률이다

Deflated Sharpe Ratio(Bailey & López de Prado, 2014)는 샤프가 아니라 [0, 1]
범위의 확률이다. 따라서 "DSR > 0"은 공허한 기준이고, 집행 기준은
`core/risk/limits.yaml`의 `deflated_sharpe_probability_min: 0.95`다. 샤프 단위의
할인폭은 `G4.metrics.deflated_sharpe_excess`로 보고만 하고 판정에 쓰지 않는다.
기준의 단일 출처는 항상 limits.yaml이며, 이 문서의 표는 그 사본이다.

## G4의 잔차 알파 t ≥ 3.0

가장 흔한 실패는 과최적화가 아니라 팩터 노출을 알파로 착각하는 것이다. FF5 +
모멘텀으로 회귀한 뒤 잔차 알파의 t를 본다. 통상 기준 2.0을 쓰지 않는 이유는
같은 수익 단면에 수백 개 팩터가 반복 검정되어 왔기 때문이다(Harvey, Liu & Zhu,
2016은 신규 팩터에 t > 3.0을 권고한다).

## G7은 성과를 판정하지 않는다

샤프 추정량의 표준오차는 √((1+SR²/2)/T)다(Lo, 2002). 일간 관측 60개에 연율 샤프
1.0을 넣으면 연율 표준오차가 약 2.05이고, 신뢰구간은 대략 −3.0 ~ +5.1이다.
그래서 G7은 슬리피지 예측 정확도(±50% 이내), 포지션 일치율(≥ 99%), 수익 트래킹
편차(≤ 백테스트 변동성의 30%), 운영 무사고만 본다. 수익률은 판정 대상이 아니다.

## 금지

- 게이트를 통과시키려고 기준을 낮추는 것. 기준 변경 PR은 캐너리 스위트
  (`tests/canaries/`)를 다시 통과해야 한다.
- 사전등록 없이 탐색한 파라미터로 제출하는 것. N을 모르면 DSR을 계산할 수 없다.
- 제안 포드가 자기 알파의 게이트를 판정하는 것.
